import { Card, CardContent, CardHeader, CardTitle } from "@kruzer-corp/ds";

export default function MembrosSegmentos() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Segmentação</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Defina segmentos estratégicos e critérios de ativação para membros.</p>
      </CardContent>
    </Card>
  );
}
