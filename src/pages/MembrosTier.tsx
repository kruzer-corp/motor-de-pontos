import { Card, CardContent, CardHeader, CardTitle } from "@kruzer-corp/ds";

export default function MembrosTier() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Estrutura de tiers</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Gerencie níveis de fidelidade, benefícios e requisitos de upgrade.</p>
      </CardContent>
    </Card>
  );
}
