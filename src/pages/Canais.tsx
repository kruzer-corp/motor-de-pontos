import { Card, CardContent, CardHeader, CardTitle } from "@kruzer-corp/ds";

export default function Canais() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Canais</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Gerencie canais de venda e regras híbridas de elegibilidade.</p>
      </CardContent>
    </Card>
  );
}
